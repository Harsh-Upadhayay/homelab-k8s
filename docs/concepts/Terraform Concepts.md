# Terraform Concepts

> Back to [[Homelab Learning Map]]

Not yet populated — Phase 3 (`terraform apply` against `pve-dell`) hasn't run yet. The code exists in `terraform/` and is referenced from `GUIDE.md`, but per this doc's convention, an entry only lands here once it's actually been run and understood hands-on, not just read.

Will cover, once Phase 3 lands: providers (`bpg/proxmox`), resources vs. data sources, the `clone` block, state (`terraform.tfstate`) and why it's gitignored, `plan`/`apply`/`destroy`, variables vs. `.tfvars`, and why provisioning (Terraform) and configuration (Ansible) are deliberately different tools here.
